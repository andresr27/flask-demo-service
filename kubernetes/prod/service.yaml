apiVersion: v1
kind: Service
metadata:
  labels:
    name: demo-service
  name: demo-service
  namespace: applications
spec:
  clusterIP:
  ports:
    - port: 80
      targetPort: 5000
  selector:
    app: flask-demo-service